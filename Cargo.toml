[package]
name = "warp-diagnose"
version = "0.1.0"
edition = "2024"
build = "build.rs"

[dependencies]
slint = "1"
anyhow = "1"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
chrono = { version = "0.4", features = ["serde"] }
regex = "1"

[build-dependencies]
slint-build = "1"
